/* Visual viewport follows mobile browser chrome and the on-screen keyboard. */
(()=>{const view=window.visualViewport;let frame;function sync(){cancelAnimationFrame(frame);frame=requestAnimationFrame(()=>{const mobile=matchMedia('(max-width:999px)').matches;const height=view&&view.scale===1?view.height:innerHeight;document.documentElement.style.setProperty('--app-height',Math.round(height)+'px');document.body.dataset.keyboard=String(mobile&&/INPUT|TEXTAREA|SELECT/.test(document.activeElement?.tagName||'')&&innerHeight-height>120);});}view?.addEventListener('resize',sync);window.addEventListener('resize',sync);document.addEventListener('focusin',sync);document.addEventListener('focusout',sync);sync();})();
