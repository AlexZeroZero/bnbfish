# 服务费候选版本（未部署）
用户确认：每竿 0.00001 BNB，网络 Gas 另计。接收地址：0x1bb935eeedd1338ebbb4faa79c1009314b8c520e.

现有主网合约保持不变。候选合约每次成功抛竿在同一交易内向固定收款地址支付服务费，金额不符或收款失败则整笔回退，不产生鱼竿。收竿不退已完成的服务费。金额为常量，收款地址为 immutable，均无管理修改入口。

切换前必须完成候选部署验证、前端金额核验及显式报价、旧 NFT 与垂钓记录入口保留，以及用户钱包部署签名。旧 NFT 不会自动迁移，新部署也不会继承旧发行量；全系列 200 万数量上限需要先确定跨版本配额方案，不能直接重新发行 200 万。当前线上仍然仅收网络 Gas。
